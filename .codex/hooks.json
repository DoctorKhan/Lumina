{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r \".tool_input.command\"); if printf \"%s\" \"$cmd\" | grep -Eq \"(^|[;&|({])[[:space:]]*(npm|npx)([[:space:]]|$)\"; then printf \"%s\" \"{\\\"hookSpecificOutput\\\":{\\\"hookEventName\\\":\\\"PreToolUse\\\",\\\"permissionDecision\\\":\\\"deny\\\",\\\"permissionDecisionReason\\\":\\\"This project uses pnpm. Use the pnpm equivalent instead (e.g. npm test -> pnpm test, npm install -> pnpm install, npx vite -> pnpm exec vite).\\\"}}\"; fi",
            "statusMessage": "Checking for npm/npx (project uses pnpm)"
          }
        ]
      }
    ]
  }
}
